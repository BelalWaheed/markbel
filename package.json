{
  "name": "markbel-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "vercel-build": "npm run build --workspace=frontend && npm run build --workspace=backend"
  }
}
